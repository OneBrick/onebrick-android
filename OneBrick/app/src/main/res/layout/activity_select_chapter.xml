<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="org.onebrick.android.activities.SelectChapterActivity">
    <ImageView
        android:layout_marginTop="20dp"
        android:id="@+id/ivCurrentLocation"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginLeft="50dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:src="@drawable/current_location"
    />
    <Button
        android:id="@+id/btnUseCurrentLocation"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_toRightOf="@+id/ivCurrentLocation"
        android:text="Use Current Location"
        android:layout_marginLeft="10dp"
        android:background="@android:color/transparent"
        android:gravity="fill_vertical"
        android:textColor="@color/charcoal"
        android:layout_alignBottom="@+id/ivCurrentLocation"/>
    <TextView
        android:id="@+id/tvOrLbl"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/ivCurrentLocation"
        android:text="or select from the below list"
        android:gravity="center"/>
    <ListView
        android:layout_marginTop="20dp"
        android:id="@+id/lvChapterPrompt"
        android:layout_below="@+id/tvOrLbl"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true">

    </ListView>

</RelativeLayout>
